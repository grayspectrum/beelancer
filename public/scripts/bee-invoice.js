/*
 * beelancer - bee-invoice.js
 * Author: Gordon Hall
 */

// handles retrieval of invoice and allows for payment

if (!('bee' in window)) {
	window.bee = {};
}

bee.invoice = (function(bee) {
	
	
	return {};
})(window.bee);
